import { CreationEventDirective } from './creation-event.directive';

describe('CreationEventDirective', () => {
  it('should create an instance', () => {
    const directive = CreationEventDirective;
    expect(directive).toBeTruthy();
  });
});
